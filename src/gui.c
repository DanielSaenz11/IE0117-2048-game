#include "../include/gui.h"
#include <SDL2/SDL.h>

void renderizarTablero(Game* game, SDL_Renderer* renderer) {
    // Implementar la función para renderizar el tablero
    // Utilizar SDL_Renderer* renderer para dibujar el tablero según el estado del juego en Game* game
    // Implementar la lógica necesaria para renderizar los elementos del juego en la ventana
}
